'use strict';

export {};

let firstList: number[] = [1, 2, 3];
let secondList: number[] = [4, 5];

if (firstList.length < secondList.length) {
    console.log(true);
} else {
    console.log(false);
}